@model Resturant

<section>
    <h3>@Model.Name</h3>
    <div> 
        Cuisine: @Model.Cuisine
    </div>
    <div>
        <a asp-action="Details" asp-route-id="@Model.Id">More</a>
        <a asp-page="/Resturants/Edit" asp-route-id="@Model.Id">Edit</a>
    </div>
</section>